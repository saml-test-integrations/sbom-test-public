name: syft-sbom-ci

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  sbom:
    uses: saml-test-integrations/sbom-test/.github/workflows/syft-release-sbom.yml@main #mozilla/ssdlc-actions/.github/workflows/syft-release-sbom.yml@main
    with:
      sbom_name: ${{ github.event.release.tag_name || 'manual' }}
